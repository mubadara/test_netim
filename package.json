{
  "name": "netim_test",
  "version": "1.0.0",
  "description": "Test intégration pour Netim",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "sass assets/styles/sass/main.sass assets/styles/style.css",
    "watch": "sass --watch assets/styles/sass/main.sass assets/styles/style.css",
    "build": "sass assets/styles/sass/main.sass assets/styles/style.min.css --style compressed"
  },
  "author": "RenaudB",
  "license": "ISC",
  "devDependencies": {
    "sass": "^1.35.1"
  }
}
